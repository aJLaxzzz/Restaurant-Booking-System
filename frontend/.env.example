# Необязательно: базовый URL для /demo/* и /api/files/*, если статика на другом домене.
# Пусто — относительные пути от текущего origin (норма для Vite dev и nginx-монолита).
# VITE_PUBLIC_ORIGIN=https://static.example.com
